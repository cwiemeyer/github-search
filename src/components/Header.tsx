import React from "react";

class Header extends React.Component {
    render() {
        return (
            <header>
                <h1>GitHub React Issue Search Helper</h1>
                <h2>
                    Search GitHub React issues and get the search results easily displayed in a list within this page.
                </h2>
            </header>
        );
    }
}

export default Header;